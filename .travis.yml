language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - PUBLIC_URL=https://kevinleung987.github.io/csc318-classmood/
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master